--[[
  VIOLENCE DISTRICT SCRIPT
  Optimized for Delta Executor (Android)
  No external libraries needed
]]

-- Variables
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- GUI System (Simple, no library)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ViolenceDistrictGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 350, 0, 500)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 2
MainFrame.BorderColor3 = Color3.fromRGB(70, 85, 130)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

-- UICorner
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(70, 85, 130)
Title.Text = "VIOLENCE DISTRICT"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextScaled = true
Title.Font = Enum.Font.SourceSansBold
Title.Parent = MainFrame

-- Tab Buttons
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1, 0, 0, 40)
TabFrame.Position = UDim2.new(0, 0, 0, 40)
TabFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
TabFrame.Parent = MainFrame

local Tabs = {"ESP", "Player", "Movement", "Misc"}
local TabButtons = {}
local CurrentTab = "ESP"

-- Content Frame
local ContentFrame = Instance.new("ScrollingFrame")
ContentFrame.Size = UDim2.new(1, -20, 1, -100)
ContentFrame.Position = UDim2.new(0, 10, 0, 90)
ContentFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
ContentFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ContentFrame.ScrollBarThickness = 5
ContentFrame.Parent = MainFrame

-- Create Tab Buttons
for i, tabName in ipairs(Tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.25, -2, 1, 0)
    btn.Position = UDim2.new((i-1) * 0.25, 1, 0, 0)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    btn.Text = tabName
    btn.TextColor3 = Color3.fromRGB(200, 200, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.SourceSansBold
    btn.Parent = TabFrame
    
    btn.MouseButton1Click:Connect(function()
        CurrentTab = tabName
        UpdateContent()
        
        -- Update button colors
        for _, b in ipairs(TabButtons) do
            b.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
        end
        btn.BackgroundColor3 = Color3.fromRGB(70, 85, 130)
    end)
    
    table.insert(TabButtons, btn)
end

-- Toggle Button (Floating)
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(0, 20, 0, 100)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(70, 85, 130)
ToggleBtn.Text = "⚡"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextScaled = true
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.Active = true
ToggleBtn.Draggable = true
ToggleBtn.Parent = ScreenGui

-- UICorner for toggle
local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleBtn

-- Toggle GUI
ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Settings
local Settings = {
    ESP = {
        Survivor = false,
        Killer = false,
        Generator = false,
        Distance = 100
    },
    Player = {
        Walkspeed = false,
        WalkspeedValue = 20,
        JumpPower = false,
        JumpPowerValue = 50,
        NoClip = false,
        GodMode = false
    },
    Auto = {
        Parry = false,
        SkillCheck = false
    }
}

-- ESP System
local ESPObjects = {}

local function CreateESP(obj, color)
    if ESPObjects[obj] then return end
    
    local highlight = Instance.new("Highlight")
    highlight.FillColor = color
    highlight.OutlineColor = color
    highlight.FillTransparency = 0.7
    highlight.OutlineTransparency = 0.3
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Parent = obj
    
    ESPObjects[obj] = highlight
    
    obj.AncestryChanged:Connect(function()
        if not obj.Parent then
            if ESPObjects[obj] then
                ESPObjects[obj]:Destroy()
                ESPObjects[obj] = nil
            end
        end
    end)
end

local function UpdateESP()
    while task.wait(0.5) do
        if not Settings.ESP.Survivor and not Settings.ESP.Killer and not Settings.ESP.Generator then
            -- Clear all ESP
            for obj, highlight in pairs(ESPObjects) do
                highlight:Destroy()
            end
            ESPObjects = {}
            continue
        end
        
        -- ESP for players
        for _, player in pairs(Players:GetPlayers()) do
            if player == LocalPlayer then continue end
            if not player.Character then continue end
            
            local root = player.Character:FindFirstChild("HumanoidRootPart")
            if not root then continue end
            
            -- Check distance
            local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if myRoot then
                local dist = (root.Position - myRoot.Position).Magnitude
                if dist > Settings.ESP.Distance then continue end
            end
            
            -- Determine if survivor or killer (simple check)
            local isKiller = false
            -- Check for weapon/tool
            for _, child in pairs(player.Character:GetChildren()) do
                if child:IsA("Tool") or child.Name:lower():find("knife") or child.Name:lower():find("weapon") then
                    isKiller = true
                    break
                end
            end
            
            if isKiller and Settings.ESP.Killer then
                CreateESP(player.Character, Color3.fromRGB(255, 50, 50))
            elseif not isKiller and Settings.ESP.Survivor then
                CreateESP(player.Character, Color3.fromRGB(50, 255, 100))
            end
        end
        
        -- ESP for generators
        if Settings.ESP.Generator then
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj.Name:lower():find("generator") or obj.Name:lower():find("gen") then
                    if obj:IsA("BasePart") or obj:IsA("Model") then
                        CreateESP(obj, Color3.fromRGB(255, 170, 0))
                    end
                end
            end
        end
    end
end

-- Player Mods
local function ApplyWalkspeed()
    while task.wait(0.1) do
        if not Settings.Player.Walkspeed then task.wait(1) continue end
        
        local char = LocalPlayer.Character
        if not char then continue end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = Settings.Player.WalkspeedValue
        end
    end
end

local function ApplyJumpPower()
    while task.wait(0.1) do
        if not Settings.Player.JumpPower then task.wait(1) continue end
        
        local char = LocalPlayer.Character
        if not char then continue end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.JumpPower = Settings.Player.JumpPowerValue
        end
    end
end

local function ApplyNoClip()
    while task.wait(0.1) do
        if not Settings.Player.NoClip then task.wait(1) continue end
        
        local char = LocalPlayer.Character
        if not char then continue end
        
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end

local function ApplyGodMode()
    while task.wait(0.1) do
        if not Settings.Player.GodMode then task.wait(1) continue end
        
        local char = LocalPlayer.Character
        if not char then continue end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 then
            hum.Health = hum.MaxHealth
        end
    end
end

-- Auto Parry
local function AutoParry()
    while task.wait(0.05) do
        if not Settings.Auto.Parry then task.wait(1) continue end
        
        -- Check for incoming attacks (simplified)
        local char = LocalPlayer.Character
        if not char then continue end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum then continue end
        
        -- Check if being attacked (health dropping)
        if hum.Health < hum.MaxHealth * 0.9 then
            -- Try to parry (press Q or parry key)
            pcall(function()
                -- Find parry remote
                local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                if remotes then
                    local parryRemote = remotes:FindFirstChild("Parry") or 
                                       remotes:FindFirstChild("Block") or
                                       remotes:FindFirstChild("Defend")
                    if parryRemote then
                        parryRemote:FireServer()
                    end
                end
            end)
        end
    end
end

-- Auto Skill Check
local function AutoSkillCheck()
    while task.wait(0.1) do
        if not Settings.Auto.SkillCheck then task.wait(1) continue end
        
        -- Find skill check remote
        local genRemote = ReplicatedStorage:FindFirstChild("Remotes")
        if genRemote then
            genRemote = genRemote:FindFirstChild("Generator")
            if genRemote then
                local skillCheck = genRemote:FindFirstChild("SkillCheckResultEvent") or
                                  genRemote:FindFirstChild("SkillCheck") or
                                  genRemote:FindFirstChild("Complete")
                if skillCheck then
                    pcall(function()
                        skillCheck:FireServer(true) -- Perfect skill check
                    end)
                end
            end
        end
    end
end

-- Create Toggle in Content
local function CreateToggle(parent, text, setting, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 35)
    frame.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    frame.Parent = parent
    
    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 5)
    UICorner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(200, 200, 255)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextScaled = true
    label.Font = Enum.Font.SourceSans
    label.Parent = frame
    
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0, 30, 0, 30)
    toggle.Position = UDim2.new(1, -35, 0, 2.5)
    toggle.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    toggle.Text = ""
    toggle.Parent = frame
    
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(1, 0)
    toggleCorner.Parent = toggle
    
    local state = false
    
    toggle.MouseButton1Click:Connect(function()
        state = not state
        toggle.BackgroundColor3 = state and Color3.fromRGB(70, 200, 100) or Color3.fromRGB(100, 100, 100)
        setting = state
        if callback then callback(state) end
    end)
    
    return frame
end

-- Create Slider
local function CreateSlider(parent, text, min, max, default, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 50)
    frame.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    frame.Parent = parent
    
    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 5)
    UICorner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -10, 0, 20)
    label.Position = UDim2.new(0, 5, 0, 2)
    label.BackgroundTransparency = 1
    label.Text = text .. ": " .. default
    label.TextColor3 = Color3.fromRGB(200, 200, 255)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextScaled = true
    label.Font = Enum.Font.SourceSans
    label.Parent = frame
    
    local slider = Instance.new("TextBox")
    slider.Size = UDim2.new(1, -20, 0, 25)
    slider.Position = UDim2.new(0, 10, 0, 22)
    slider.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
    slider.Text = tostring(default)
    slider.TextColor3 = Color3.fromRGB(255, 255, 255)
    slider.TextScaled = true
    slider.Font = Enum.Font.SourceSans
    slider.ClearTextOnFocus = false
    slider.Parent = frame
    
    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 5)
    sliderCorner.Parent = slider
    
    slider.FocusLost:Connect(function()
        local value = tonumber(slider.Text)
        if value then
            value = math.clamp(value, min, max)
            slider.Text = tostring(value)
            label.Text = text .. ": " .. value
            if callback then callback(value) end
        end
    end)
    
    return frame
end

-- Update Content based on tab
function UpdateContent()
    -- Clear content
    for _, child in pairs(ContentFrame:GetChildren()) do
        child:Destroy()
    end
    
    local yOffset = 5
    
    if CurrentTab == "ESP" then
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 25)
        title.BackgroundTransparency = 1
        title.Text = "ESP Settings"
        title.TextColor3 = Color3.fromRGB(70, 85, 130)
        title.TextScaled = true
        title.Font = Enum.Font.SourceSansBold
        title.Parent = ContentFrame
        yOffset = yOffset + 30
        
        CreateToggle(ContentFrame, "Survivor ESP", Settings.ESP.Survivor, function(state)
            Settings.ESP.Survivor = state
        end)
        
        CreateToggle(ContentFrame, "Killer ESP", Settings.ESP.Killer, function(state)
            Settings.ESP.Killer = state
        end)
        
        CreateToggle(ContentFrame, "Generator ESP", Settings.ESP.Generator, function(state)
            Settings.ESP.Generator = state
        end)
        
        CreateSlider(ContentFrame, "ESP Distance", 10, 500, Settings.ESP.Distance, function(value)
            Settings.ESP.Distance = value
        end)
        
    elseif CurrentTab == "Player" then
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 25)
        title.BackgroundTransparency = 1
        title.Text = "Player Mods"
        title.TextColor3 = Color3.fromRGB(70, 85, 130)
        title.TextScaled = true
        title.Font = Enum.Font.SourceSansBold
        title.Parent = ContentFrame
        
        CreateToggle(ContentFrame, "Walkspeed", Settings.Player.Walkspeed, function(state)
            Settings.Player.Walkspeed = state
        end)
        
        CreateSlider(ContentFrame, "Walkspeed Value", 16, 100, Settings.Player.WalkspeedValue, function(value)
            Settings.Player.WalkspeedValue = value
        end)
        
        CreateToggle(ContentFrame, "Jump Power", Settings.Player.JumpPower, function(state)
            Settings.Player.JumpPower = state
        end)
        
        CreateSlider(ContentFrame, "Jump Power Value", 50, 200, Settings.Player.JumpPowerValue, function(value)
            Settings.Player.JumpPowerValue = value
        end)
        
        CreateToggle(ContentFrame, "NoClip", Settings.Player.NoClip, function(state)
            Settings.Player.NoClip = state
        end)
        
        CreateToggle(ContentFrame, "God Mode", Settings.Player.GodMode, function(state)
            Settings.Player.GodMode = state
        end)
        
    elseif CurrentTab == "Movement" then
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 25)
        title.BackgroundTransparency = 1
        title.Text = "Auto Features"
        title.TextColor3 = Color3.fromRGB(70, 85, 130)
        title.TextScaled = true
        title.Font = Enum.Font.SourceSansBold
        title.Parent = ContentFrame
        
        CreateToggle(ContentFrame, "Auto Parry", Settings.Auto.Parry, function(state)
            Settings.Auto.Parry = state
        end)
        
        CreateToggle(ContentFrame, "Auto Skill Check", Settings.Auto.SkillCheck, function(state)
            Settings.Auto.SkillCheck = state
        end)
        
    elseif CurrentTab == "Misc" then
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 25)
        title.BackgroundTransparency = 1
        title.Text = "Miscellaneous"
        title.TextColor3 = Color3.fromRGB(70, 85, 130)
        title.TextScaled = true
        title.Font = Enum.Font.SourceSansBold
        title.Parent = ContentFrame
        
        local info = Instance.new("TextLabel")
        info.Size = UDim2.new(1, 0, 0, 100)
        info.Position = UDim2.new(0, 0, 0, 30)
        info.BackgroundTransparency = 1
        info.Text = "Violence District Script\nOptimized for Delta Android\n\nPress the ⚡ button to toggle menu"
        info.TextColor3 = Color3.fromRGB(150, 150, 200)
        info.TextScaled = true
        info.Font = Enum.Font.SourceSans
        info.Parent = ContentFrame
    end
    
    -- Update canvas size
    ContentFrame.CanvasSize = UDim2.new(0, 0, 0, yOffset + #ContentFrame:GetChildren() * 40)
end

-- Initialize
UpdateContent()

-- Start threads
spawn(UpdateESP)
spawn(ApplyWalkspeed)
spawn(ApplyJumpPower)
spawn(ApplyNoClip)
spawn(ApplyGodMode)
spawn(AutoParry)
spawn(AutoSkillCheck)

-- Keybind (LeftControl to toggle)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.LeftControl then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

print("✅ Violence District Script Loaded!")
print("📱 Press ⚡ button or LeftControl to open menu")
